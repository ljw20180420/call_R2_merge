# Introduction

Find `R2` with the same physical address as `R1`.

# Usage

```shell
./call_r2_merge -R1 R1.fq -R2 R2.fq -source 下机.R2.fq -sta 截取起始 -end 截取终止
```

# TODO

- [] Build debug and release separately to allow easy debug.
